"use client";

/**
 * Convenience barrel for pages that need primitives and overlay controls
 * (switch, checkbox, select) without importing three modules.
 */
export {
  Badge,
  Button,
  Card,
  CardContent,
  CardHeader,
  CardTitle,
  EmptyState,
  FieldError,
  HelpText,
  Input,
  Label,
  LinkButton,
  Skeleton,
  Spinner,
  Textarea,
} from "./primitives";

export { Checkbox, Select, SelectContent, SelectItem, SelectTrigger, SelectValue, Switch } from "./overlays";
